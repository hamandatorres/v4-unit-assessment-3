import React from 'react';

export default function Shelf() {
  return (
    {const mappedTitles = props.shelf.map((e) => {
      return
      <h1></h1>
    })}
    <div>
    <h1>Shelf</h1>
    <button onClick={ props.clearShelf } >
    Clear Shelf
    </button>
    </div>
  )
}